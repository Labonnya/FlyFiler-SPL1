void registration()
{
    string name,email_id,password,user_name;
    int phone_no,date_of_birth;
    cout<<"Fill up the following informations to sign up with us today.";
    cout<<"\n";
    cout<<"Enter your name: ";
    cin>>name;
    int count=0;
    for(int i=0;name[i]!='\0';i++){
      count++;
      if(name[i]!=)

   }


}
